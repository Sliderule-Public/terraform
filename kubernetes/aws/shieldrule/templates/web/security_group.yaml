apiVersion: vpcresources.k8s.aws/v1beta1
kind: SecurityGroupPolicy
metadata:
  name: shieldrule-sg-web
spec:
  podSelector:
    matchLabels:
      role: shieldrule-web-role
  securityGroups:
    groupIds:
      - {{ .Values.pod_security_group }}