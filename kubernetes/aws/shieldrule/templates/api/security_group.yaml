apiVersion: vpcresources.k8s.aws/v1beta1
kind: SecurityGroupPolicy
metadata:
  name: shieldrule-sg-api
spec:
  podSelector:
    matchLabels:
      role: shieldrule-api-role
  securityGroups:
    groupIds:
      - {{ .Values.pod_security_group }}