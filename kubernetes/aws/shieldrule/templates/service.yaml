apiVersion: v1
kind: Service
metadata:
  name: {{ include "shieldrule.fullname" . }}
  labels:
    {{- include "shieldrule.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shieldrule.selectorLabels" . | nindent 4 }}
